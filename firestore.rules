rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ... 其他規則 ...
    
    // 允許已登入（包含匿名）的使用者寫入郵件集合，並允許讀取以追蹤發送狀態
    match /mail/{document=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
  }
}