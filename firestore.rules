rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for everyone for debugging purposes
    // In production, you should lock this down!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
